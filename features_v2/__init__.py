"""v2 feature engineering — 174-feature catalog (H001..H174).

Modules land here in P2: catalog (registry), compute (per-feature pure functions),
pipeline (orchestration with point-in-time snapshot enforcement).
"""
