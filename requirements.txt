# Runtime dependencies for racing.service. The systemd unit runs the SYSTEM
# python (/usr/bin/python3) with ProtectHome=true, so these must be installed
# SYSTEM-WIDE:  sudo /usr/bin/python3 -m pip install -r requirements.txt
# (see deploy/install.sh / deploy/DEPLOY.md).

# Web stack
fastapi==0.115.8
uvicorn
httpx==0.28.1

# Real-time UI: Socket.IO server backed by Redis so out-of-process jobs
# (cron scripts) can publish progress via a write-only RedisManager.
python-socketio==5.16.2
python-engineio==4.13.2
redis==8.0.0
# Pinned to keep httpcore (httpx) and engineio's websocket deps compatible:
# httpcore 1.0.7 needs h11<0.15; wsproto>=1.3 would force h11>=0.16.
h11==0.14.0
wsproto==1.2.0

# Model + features
xgboost==3.2.0
numpy==2.2.6
scikit-learn==1.7.2
pandas==2.3.3

# Scrapers (HTML parsing)
beautifulsoup4==4.12.3
lxml==4.8.0
