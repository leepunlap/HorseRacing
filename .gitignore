# Data files
data/*.csv
data/*.db
data/*.db-wal
data/*.db-shm
data/raw_results_CN/
data/*.tar.gz
data/*.json
# Scraper runtime state (regenerable)
data/checkpoints/
data/raw/
data/v2_checkpoints/

# Python artifacts
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Logs
*.log

# Chat logs
chat_notes/

# Model results (generated by backtest, not source)
models/*/results/

# Predictions cache (generated by scraper/backtest)
predictions/

# Generated reports (HTML + PDF) — regenerable via
# scripts/build_tonights_report.py
reports/

# Sweep CSVs from scripts/sweep_hyperparams.py — regenerable
data/sweep_results_*.csv

# Quick-eval intermediate JSON (the surviving-feature list) — regenerable
# via scripts/audit_features.py --json
data/usable_features.json

# Local Claude Code settings (machine-specific)
.claude/settings.local.json

# Secrets / API keys — never commit
.env
.env.*
data/*.sqlite
data/*.sqlite3
