# Session log — 2026-05-30

Branch `feature/live-status-dashboard` (13 commits, all pushed to origin).
Everything below is live on **https://horseracing.privatedns.org/** (systemd
service `racing.service` on **port 8006**).

---

## 1. Real-time infrastructure (Socket.IO + Redis)  — `2427155`, `40f5aa3`

- Replaced the raw `/ws/progress` WebSocket with a **Socket.IO server backed by
  Redis** (`AsyncRedisManager`), mounted in front of FastAPI as `app:asgi`.
  This lets **out-of-process jobs (cron scripts) stream to the UI** without
  importing the app — they publish to Redis, the server fans out to browsers.
- `utils/emit.py` — fire-and-forget log emitter for scripts.
- `status.py` — structured **process/task status layer**: producers
  (`process_up/heartbeat/task_start/step/done/error`) publish to a `hr:status`
  Redis channel; an in-app consumer folds events into a registry and forwards
  to browsers. `GET /api/status` returns the snapshot.
- Client pinned to WebSocket-only transport to silence polling-upgrade noise.
- Dep note: pinned `h11==0.14.0` / `wsproto==1.2.0` so python-socketio coexists
  with httpx. Installed **system-wide** (the systemd unit has `ProtectHome=true`,
  so user-site packages are invisible to it).

## 2. Live status dashboard  — `2427155`, `5c2a595`, `01f3704`

Full overhaul. Panels (top→bottom):
- **Upcoming Race** — full-width pipeline-readiness pane (see §6).
- **Processes** — one card per loop/job: running state, ticking uptime,
  heartbeat age, error count, current activity.
- **Tasks (live)** — to-do list grouped by process: progress bar + ETA,
  strike-through on done, red on error. Strikes off as work proceeds.
- **Scheduled tasks** — the cron schedules (cadence, action, last-fired status).
- **Data sources** — per-scraper bilingual description, record count, date range,
  source URL.
- Dropped the betting-control panels (kill-switch/live-mode/circuit-breakers) and
  the redundant progress log.
- Every loop + scraper + the integrity check is **status-instrumented**.

## 3. Deployment  — `9159346`

- nginx `horseracing.privatedns.org` → `127.0.0.1:8006`; systemd unit updated to
  `--port 8006` (8005 is held by an unrelated `PayAll` service). TLS cert
  (`lvoyage.aero` multi-domain) covers the host; DNS resolves publicly.
- **Out-of-repo server state** (re-apply on a rebuild): the nginx vhost, the
  system-wide pip installs (`python-socketio redis h11<0.15 wsproto<1.3`), and
  the `schedules.json` entries.

## 4. Feature importance & stats page  — `8d89cb6`

- New **"Importance & stats"** tab on Features, **selectable by strategy**.
- One card per feature (all 195), sorted by **XGBoost gain**, bilingual:
  name + description (+ technical/layman notes), compute logic/params/sources,
  enabled state, gain bar + split count, and value stats (coverage/mean/min/max).
- `GET /api/strategies/{id}/feature_cards`; importance cached by
  `scripts/compute_feature_importance.py` (rerun after model changes).
- Top features for `model_lambda_v1`: **H181** field-rating-z, H090 last-finish,
  H091 last-lengths-behind, H135 jockey×venue, H009 rating.

## 5. Model tuning  — `bd8dea8`  (+ research, no code)

- **Wired `strategies.xgb_params_json`** into `walk_forward` — it was **dead**
  (the live ranker always used hardcoded defaults). Now tunable without code.
- **Enabled Stage-2 Benter** (market blend). Validated A/B on 49 recent races:
  top-1 **34.7% → 49.0% (+14.3pp)**, log-loss 0.2224 → 0.1906. Now live (α=1.0,
  β=0.9). *Caveat:* this is accuracy/calibration, not proven betting edge — it
  leans the ranking toward the market favourite.
- **β sweep**: β=0.9 confirmed optimal for top-1 and log-loss (unimodal peak).
- **RCA tags are NOT a top-1 lever** (two independent checks):
  - Diagnostic: 20/25 recent missed winners had *clean* form; 22/25 were the
    model's #2–#5. Top-1 is lost re-ranking clean contenders, not to troubled
    horses. RCA's only top-1 value is the "forgiveness" lens (true skill ~55%).
  - A "forgiveness" training-label experiment **hurt** top-1 (−4.1pp) → reverted.

## 6. Upcoming-race pipeline + race-page enrichment

- **`/api/upcoming`** + dashboard pane: next meeting's readiness across
  card → features → predictions → odds (stage 2) → bets, with a `core_ready`
  flag. Reflects the two-stage card (odds/bets "await stage 2"). — `01f3704`
- **`prepare_upcoming`** chain — scrape card → clean stubs → **barrier trials**
  → features → predictions. Registered as a scheduler action with a **daily
  05:00 schedule**. — `1006284`, `ce40efb`
- **`scrape_race_card` fixes** — `9204bd8`, `60fbb10`:
  - Stub bug: `--next` no longer inserts 1-race shells for non-meeting days.
  - **Chinese names** via horse links (`horseid` → zh profile page `<title>`).
  - **Debutant enrichment**: pedigree (sire/dam) from the profile page feeds
    H178/H179 (sire WR) — **no retrain needed** (the model already learned those
    weights from pedigreed horses).
- **Race-page flags** — `dfd7e3c`, `ce40efb`: `prior_starts` →
  **debut / low-data** badges; latest **barrier trial** chip (green if placed) —
  a debutant now reads e.g. "debut · trial 2".
- **AI news preview** — `a219390`: Google News RSS → DeepSeek → bilingual meeting
  preview + tipped runners (matched to brand), stored per meeting,
  displayed on the race page as **advisory only — never a model input**.

## Current operational state

- `racing.service` active on 8006; daily schedules: 04:30 integrity, **05:00
  prepare_upcoming**, multi-leg dividends.
- Tomorrow's meeting **2026-05-31 ST** is `core_ready: True` (card 145 runners,
  features, predictions all done; 14 debutants enriched with pedigree + trials;
  AI preview generated). Stage = 1 (no odds yet).
- Production model: `model_lambda_v1`, LambdaMART (`rank:ndcg`), 84 features,
  Stage-2 on (β 0.9), isotonic calibration, τ=180d.

---

## Suggested next steps

**Model / prediction**
1. **Measure betting edge, not just top-1.** Stage-2 lifted accuracy by leaning
   on the favourite; the real test is ROI / CLV on the bet ledger. Add a
   walk-forward ROI report alongside top-1.
2. **Top-2–5 re-ranking** is where top-1 is won (88% of missed winners sit
   there). Try: speed-figure features (H175–177), field-rating-z (already #1),
   XGB capacity (depth/rounds — now tunable), and a `min_child_weight`/`gamma`
   sweep.
3. **Refresh feature importance** after any model change
   (`python3 -m scripts.compute_feature_importance 1`); consider scheduling it.

**Upcoming-race pipeline**
4. **Stage-2 odds re-pull**: once HKJC opens odds, re-run predictions WITH the
   market blend (currently stage-1 predictions are fundamental-only). Wire an
   odds-driven re-predict into the live loop or a stage-2 schedule.
5. **Verify the live race-day loops** actually fire for a real meeting
   (odds_poller → decision_loop → post_settle) — not yet observed end-to-end.
6. **Debutant data**: 2/14 debutants still lack sire WR (rare sires); barrier
   H094 covers 13/14. Consider overseas-form / dam-side signals for the rest.

**Ops / robustness — ✅ DONE**
7. ✅ **Server-side state captured into `deploy/`** — `nginx-horseracing.conf`,
   `schedules.seed.json`, completed `requirements.txt`, `install.sh` (idempotent),
   `DEPLOY.md`. One-shot rebuild: `sudo bash deploy/install.sh`.
8. ✅ **Reboot survival** — racing / nginx / redis-server all `enabled`; the unit
   is now ordered `After=redis-server.service` so it never starts before its
   Redis dependency on a cold boot.
9. ✅ **Merged `feature/live-status-dashboard` → master** (now at `55ea64c`).

**AI news overlay (optional)**
10. Wire `fetch_race_news` into `prepare_upcoming` for the upcoming meeting; add
    a better racing-specific source than general Google News; keep it advisory.
